import React from 'react';

const Dashboard = () => (
  <div className="dashboard">
    <h1 className="heading__main">Dashboard Page</h1>
    <p className="body-text">This is what logged in users will see</p>
  </div>
);

export default Dashboard;

import React from 'react';

const History = () => (
  <div className="history">
    <h1 className="heading__main">History Page</h1>
    <p className="body-text">This is where logged in users can view their previous entries.</p>
  </div>
);

export default History;
